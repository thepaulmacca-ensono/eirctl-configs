contexts:
  powershell:
    container:
      name: ensono/eir-infrastructure
      container_args:
        - -v ~/.azure:/root/.azure
      shell: pwsh
      shell_args:
        - -Command
    envfile:
      exclude:
        - sourceversionmessage
        - posh_azure_subscription
